{
    "name": "opal-client-4",
    "version": "4.3.0",
    "description": "ev.io client with a host of optimizations",
    "main": "out/index.js/",
    "author": "jake",
    "license": "MIT",
    "private": false,
    "scripts": {
        "start": "yarn run compile && electron . ",
        "build": "yarn run compile && yarn run packager",
        "packager": "yarn run compile && node packager.mjs",
        "compile": "yarn run format && yarn run bundle",
        "bundle": "node esbuild.mjs",
        "format": "yarn biome check --write"
    },
    "devDependencies": {
        "@biomejs/biome": "1.9.4",
        "@types/electron": "^1.6.10",
        "electron": "33.0.2",
        "electron-packager": "^17.1.1",
        "esbuild": "^0.24.0",
        "typescript": "^5.6.3"
    },
    "dependencies": {
        "v8-compile-cache": "^2.3.0"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}
